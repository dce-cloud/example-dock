services:

### TRAEFIK WHOAMI ##############################################
  whoami1:
    build:
      context: ${WHOAMI_HOST_PATH}
      args:
        DOCKER_REGISTRY_URL: ${DOCKER_REGISTRY_URL}
        WHOAMI_VERSION: ${WHOAMI_VERSION}
    container_name: ${WHOAMI1_CONTAINER_NAME}
    hostname: ${WHOAMI1_HOSTNAME}
    restart: ${DOCKER_RESTART_POLICY}
    environment:
      TZ: ${TZ}
    networks:
      backend:
        ipv4_address: ${WHOAMI1_BACKEND_IP}

### TRAEFIK WHOAMI 2 ##############################################
  whoami2:
    build:
      context: ${WHOAMI_HOST_PATH}
      args:
        DOCKER_REGISTRY_URL: ${DOCKER_REGISTRY_URL}
        WHOAMI_VERSION: ${WHOAMI_VERSION}
    container_name: ${WHOAMI2_CONTAINER_NAME}
    hostname: ${WHOAMI2_HOSTNAME}
    restart: ${DOCKER_RESTART_POLICY}
    environment:
      TZ: ${TZ}
    networks:
      backend:
        ipv4_address: ${WHOAMI2_BACKEND_IP}
